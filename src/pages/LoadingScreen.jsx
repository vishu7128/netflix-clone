import "./LoadingScreen.css"; // Assuming you have the CSS for the loading screen

const LoadingScreen = () => {
  return (
    <div className="loading-screen">
      <div className="loading-spinner"></div>
    </div>
  );
};

export default LoadingScreen;
